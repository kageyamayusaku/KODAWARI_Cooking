<!DOCTYPE html>
<html>
  <head>
    <title>KODAWARICooking</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= stylesheet_pack_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>

    <!-- Google Fonts導入のlink -->
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Zen+Kurenaido&display=swap" rel="stylesheet">

    <link href="https://fonts.googleapis.com/css?family=Caveat" rel="stylesheet">
  </head>

  <body class="d-flex flex-column vh-100">
    <% if user_signed_in? %>
    <header class="shadow-sm">
      <nav class="navbar navbar-expand-lg navbar-light">
        <div class="container">
          <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNavDropdown"aria-controls="navbarNavDropdown" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
          </button>
          <div class="collapse navbar-collapse" id="navbarNavDropdown">
            <ul class="navbar-nav ml-auto">
              <li class="nav-item mr-2">
                <%= link_to "ホーム", home_path, class: "btn btn-outline-secondary" %>
              </li>
              <li class="nav-item mr-2">
                <%= link_to "レシピ一覧", recipes_path, class: "btn btn-outline-secondary" %>
              </li>
              <li class="nav-item mr-2">
                <%= link_to "新規投稿", new_recipe_path, class: "btn btn-outline-secondary" %>
              </li>
              <li class="nav-item mr-2">
                <%= link_to "あなたのレシピ", index_user_recipes_path, class: "btn btn-outline-secondary" %>
              </li>
              <li class="nav-item">
                <%= link_to "ログアウト", destroy_user_session_path, method: :delete, class: "btn btn-outline-secondary" %>
              </li>
            </ul>
          </div>
        </div>
      </nav>
    </header>
    <% end %>
    <main>
      <div class="text-center">
        <div class="bg-primary text-white">
          <%= notice %>
        </div>
        <div class="bg-warning text-dark">
          <%= alert %>
        </div>
      </div>
      <%= yield %>
    </main>
    <footer class="bg-dark text-white mt-5 pt-5 pb-4">
      <div class="container">
        <div class="row no-gutters">
          <% unless user_signed_in? or admin_signed_in? %>
            <h5><%= link_to "管理者ログイン", new_admin_session_path %></h5>
          <% end %>
        </div>
      </div>
    </footer>
  </body>
</html>